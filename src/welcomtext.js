const welcome = (number, groupname) => {
    return `ola pana @${number}. bienvenido al grupo: ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `adios joto @${number}. ya estorbabas👋`
}
exports.bye = bye